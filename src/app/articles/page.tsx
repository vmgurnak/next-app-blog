import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'App Blog | Articles',
  description: 'App Blog Articles Page',
};

const ArticlesPage = () => {
  return <div>Articles</div>;
};

export default ArticlesPage;
